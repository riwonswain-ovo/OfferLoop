# `competency-lab` 退役说明

`competency-lab` 已退役，不再作为 OfferLoop 用户可见 Skill 安装或调用；历史内容只读保留。

- 历史岗位能力地图、专项训练、每日三题、周报和 `AbilityObservation` 原样保留，不删除、不再更新。
- `mock-lab` 保留本场评价、后续训练建议和逐题练习，但不维护跨场次能力地图。
- `talk-review` 在当前复盘文档中保留训练建议，表达与承压练习路由到 `mock-lab`。
- 本目录的历史参考仅供 Git 审计与回滚，不得作为新产物写入契约。
