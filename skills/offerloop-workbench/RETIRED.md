# 旧 OfferLoop 工作台退役说明

该工作台于 2026-08-17 冻结并退役。

- `assets/workbench-template` 仅作为历史源码快照保留，不再构建、测试、发布或维护。
- `scripts/materialize_workbench.py` 会拒绝新的铺设请求，防止误部署旧实现。
- 旧工作台不是 OfferLoop 9 个正式 Skills 的组成部分，也不再属于完整模式或一键部署流程。
- 求职企业清单、求职进展、笔面试中心、即时同步与每日提醒继续独立运行，不依赖该工作台。
- 新工作台应从新的产品合同、数据边界和独立妙搭应用开始，不在此模板上增量修补。

保留本目录是为了审计历史交互与迁移有价值的设计素材，不代表继续支持其 OAuth、数据库、页面或发布合同。
