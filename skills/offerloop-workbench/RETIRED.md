# 旧 OfferLoop 工作台退役说明

该工作台于 2026-08-17 冻结并退役。

- 旧模板源码已从活动代码树移除；历史实现仍可通过 2026-08-17 以前的 Git 提交审计。
- `scripts/materialize_workbench.py` 会拒绝新的铺设请求，防止误部署旧实现。
- 旧工作台不是 OfferLoop 7 个长期 Skill 的组成部分，也不再属于完整模式或一键部署流程。
- 求职企业清单、求职进展、笔面试中心和即时同步继续独立运行，不依赖该工作台。
- 新工作台应从新的产品合同、数据边界和独立妙搭应用开始，不在此模板上增量修补。

保留本说明和拒绝部署脚本，是为了安全识别旧安装，不代表继续支持其 OAuth、数据库、页面或发布合同。
