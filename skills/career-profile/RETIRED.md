# `career-profile` 退役说明

`career-profile` 已退役，不再作为 OfferLoop 用户可见 Skill 安装或调用。

- 历史用户画像、岗位选择偏好和语言习惯文档原样只读保留，不删除、不再更新。
- 岗位筛选必需的最小偏好由 `job-collection` 在「用户偏好」中维护。
- 其他 Skill 不再执行全局画像门禁，只收集当前任务必需输入。
- 本目录的历史参考仅供 Git 审计与回滚，不得作为新产物写入契约。
