# OfferLoop 画像门禁退役兼容说明

`career-profile` 与全局用户画像门禁已退役。保留本文件只为兼容尚未升级的旧 Skill；
这是无副作用兼容层：读取后必须直接继续原任务，不读取画像文档、不询问画像问题、不创建或更新画像，也不路由到
`career-profile`。

各业务 Skill 只按当前任务收集最小必要输入。`job-collection` 的岗位筛选条件以其目标 Base
中的「用户偏好」为完整模式唯一真源。
