# OfferLoop 线上核心链路冒烟验收

日期：2026-08-17。环境：用户当前飞书租户，`codex` profile，bot 身份。范围：求职企业清单、
求职进展、笔面试中心及其当前启用的同步 workflow。测试使用唯一标记
`OfferLoop E2E｜20260817-A01`，仅创建临时记录；验收结束后按 record ID 精确删除。

## 结果

| 验收项 | 操作 | 预期 | 实际结果 |
| --- | --- | --- | --- |
| 企业主表到分类子表 | 主表 `待确认 → 感兴趣` | “其他私企”子表跟随 | 通过 |
| 分类子表到企业主表 | 子表 `感兴趣 → 已投递` | 主表跟随 | 通过 |
| 已投递生成求职进展 | 子表改为 `已投递` | 生成唯一默认进展 | 通过 |
| 默认进展字段 | 读取自动生成记录 | `待反馈 / 投递完成` | 通过 |
| 单表多视图 | 创建一面安排 | 同时出现在“全部安排”和“一面”视图 | 通过 |
| 面试完成回写 | 一面 `待完成 → 已完成` | 进展变为 `待反馈 / 一面完成` | 通过 |

自动生成的求职进展使用 `enterprise:<企业主表 record ID>:default` 作为投递记录 ID，且正确保留
企业主表 record ID。笔面试安排通过 `求职记录ID` 精确关联该进展，没有按公司或岗位猜测关联。

## 测试对象与清理

本次创建并删除以下四条记录：

- 企业主表：`recvsxKVOXIieK`
- “其他私企”分类子表：`recvsxL0KXWQvP`
- 求职进展：`recvsxLpxeFWTx`
- 笔面试安排：`recvsxLJpT1tqF`

删除前逐条读回公司标记、来源邮件 ID、主子表双向 record ID 和求职记录关联；确认完全匹配后，
依次删除笔面试安排、求职进展、分类子表和企业主表。删除后分别在四张目标表搜索唯一公司标记或
来源邮件 ID，均返回 0 条。未修改真实记录、字段、视图、workflow、应用发布或 Git 分支。

## 过程观察

第一次创建笔面试安排的请求包含两个不属于当前 schema 的空字段名，飞书在写入前拒绝请求，未产生
部分记录。重新读取字段后，按线上实际字段合同重试成功。仓库当前代码和文档使用的字段名为 `链接`
和 `已建日程ID`，没有发现需要修复的旧字段引用。

## 未覆盖范围

本次没有发送或解析真实招聘邮件，没有创建或更新飞书日历事件，没有发送每日群卡片、任务或通知，
也没有进行压力、性能、权限越权或并发写入测试。因此本记录只证明上述受控核心数据链路在本次环境和
时间点通过，不代表所有外部集成及边界条件均已验收。

## 仓库回归验证

- 主仓库 Python：168 项通过。
- 独立 `job-progress-sync` 服务：53 项通过。
- 当前线上同步模板：21 项通过。
- `git diff --check`：通过。
