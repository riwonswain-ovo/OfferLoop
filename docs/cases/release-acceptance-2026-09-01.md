# OfferLoop 0.1.0-alpha.15 隔离安装与自动化验收

执行完成：2026-09-02。状态：候选验收通过；公开 PR #31 必须保持 CI 全绿，本文件不授权合并、创建 tag 或发布 Release。

## 验收边界

- 所有本地安装、重装、升级和回滚都在独立 HOME、Agent 目录、XDG 配置、状态目录与临时目录中执行。
- 线上写入只发生在用户明确授权的 alpha.15 隔离知识库根节点、三张隔离 Base、隔离测试群、隔离测试应用和隔离测试日历中。
- 既有 alpha.14 验收树、真实用户 Base、真实群聊、个人日历、成员、权限和分享状态均未修改。
- 报告不保存 Wiki/Base token、record ID、用户 ID、应用凭证或本机真实路径。

## 安装包与本地流程

- `OfferLoop-v0.1.0-alpha.15.zip` 最终大小为 1,247,982 字节，SHA-256 为
  `13dbde3a29a121e82295f52fba769ab501379375f3d2eb5e611d5c1af1ddf419`。
- 两次独立构建的 ZIP、manifest 和校验文件逐字节一致；包内 422 个 payload 条目全部通过白名单、逐文件摘要、路径穿越、符号链接和 2 MiB 上限检查。
- README 的真实 `scripts/setup_offerloop.py` 入口在 Codex、Claude Code、Hermes Agent 和 WorkBuddy 四类隔离环境中通过；覆盖空白和非空 Skill 目录、冲突停止、幂等重装、显式升级和安装核验。
- 缺少 `lark-cli` 与官方 Lark Skills 时，7 个本地 Skill 和隐藏运行时仍完整落盘，未触发 `npm`、`npx` 或其他网络安装；补齐依赖后，阶段状态正确推进到飞书初始化。
- 公开仓 Python 测试 191 项、job-collection 测试 139 项、recruiting-reminder 测试 103 项、核心同步服务 Node 测试 60 项全部通过。
- 妙搭模板 Jest 测试 46 项、TypeScript 类型检查和生产构建全部通过；其中包含指定日历不可见时失败关闭、`writer` 日历不新建替代日历且不邀请所有者的回归测试。
- alpha.12 的 9 Skill 安装已在第二个沙箱中完成冲突预演、alpha.15 升级、schema v7 迁移、快照回滚和恢复重试；用户哨兵与非敏感 locator 保持不变。
- 最终 ZIP 重新覆盖升级隔离 Codex 后，`setup_offerloop.py --verify` 返回 `ready/verified=true`，工作区、同步自动化和每日卡片均为 `ready`；`install_offerloop.py --verify` 同时确认本地 manifest 与 7 个 Skill 完整。

## 飞书隔离冷启动

- 在授权的测试首页下创建独立 alpha.15 验收根节点、连续的 `00`–`06` 目录、三张 Wiki 内 Base 和各 Skill 对应的合成文档。
- 企业清单主表及五张分类子表保持统一 13 字段；求职进展为 schema v6、11 字段和 13 个受管视图；笔面试中心包含 23 字段安排表、内部运行状态表和 9 个受管视图。
- 企业主表与五张分类子表的 10 条双向 workflow 已启用并真实验证：子表状态可回写主表，主表状态也可同步到对应子表，测试后原记录已恢复。
- 企业清单进入“已投递”会在求职进展中幂等创建或更新记录；笔面试中心完成状态只推进精确关联的求职记录。两条跨 Base workflow 均已真实触发、回读并恢复原测试记录。
- 同步服务已发布并通过健康检查；自动化清单回读为 12 条 enabled workflow，脱敏合同校验返回 `ready`。
- 每日卡片按 `22:10 Asia/Shanghai` 配置；隔离测试群已收到真实交互卡片，签名回调完成 Base 写入、关联进度推进和结果卡发送，随后测试记录已恢复。
- 日历只使用显式配置且已授予应用 `writer` 权限的隔离测试日历。真实“调整日程”回调创建并回读了一条 60 分钟合成日程，参与人数量为 0；没有写入个人日历，也没有静默创建替代日历。
- 为便于人工复核，唯一的日历合成记录和对应测试日程按计划保留；其余可逆状态测试均已恢复。

## 发布门禁

- 公开候选保持为相对 `main` 的单提交，并通过公开边界、隐私、确定性构建与完整 CI 检查后，才可交付人工确认。
- 在用户再次明确确认前，不合并公开 PR，不创建 `v0.1.0-alpha.15` tag，不上传 Release 资产。
