# OfferLoop 0.1.0-alpha.14

候选日期：2026-08-27。完成真实飞书冷启动验收并同步公开仓后再标记为正式发布。

本版本面向普通求职用户提供 7 个长期 Skill，并统一作为飞书一体化求职系统使用。

## 新增

- `talk-review` 的 completed 复盘成功回填到唯一关联面试事件时，会在同一次原子更新中把事件标记为
  `已完成`，再由既有即时 workflow 将求职进展更新为对应“X面完成”；没有下一轮安排时进入
  `待反馈`。
- incomplete 复盘只回填文档，不推进求职进展；已错过、已取消、文档冲突和无法唯一关联的事件安全
  停止，不静默覆盖。

## 修复

- 招聘信息同步改为有界的增量扫描。同步会从上次水位附近保留少量重叠区间，只读取新增或可能更新的记录，避免数据量增长后每次扫描全部历史记录。
- 增量水位只在本次处理成功后推进；中断或失败不会跳过尚未完成的记录。
- 补充增量扫描、去重和水位恢复测试。

## 产品边界整理

- 旧版可视化 Workbench 已正式退役，冻结模板源码不再随仓库发布；旧安装仍可被安装器识别并安全备份。
- 每日进展确认保留在活动同步模板中，固定为 `22:10 Asia/Shanghai`；只有显式启用并完成群聊、
  所有者和日历配置后才发送。笔面试完成状态继续直接在 `笔面试中心` 维护，并即时同步到关联求职进展。
- 公开 README 只介绍 7 个长期 Skill、必需的飞书工作区和面向用户的完整安装流程。
- `career-profile` 与 `competency-lab` 进入 Registry 终态 `retired`；历史文档和观察只读保留。
- 停止宣传和提供新的单 Skill 安装入口；遗留 `single` 配置升级时必须接入完整飞书工作区。

## 升级

继续使用现有升级命令。安装器会保留用户文件、三张 Base、知识库内容、邮箱去重状态和非敏感 locator；发现旧 Workbench 安装目录时会先创建可恢复备份，不会静默覆盖。

## 验证

- Python 3.10 和 3.12 仓库测试。
- 7 个 Skill 的完整安装、升级和合成联动契约验证；真实飞书冷启动在正式发布前单独验收并记录。
- 招聘信息增量扫描及其恢复测试。
- 求职进展同步服务和活动妙搭模板的单元测试、类型检查与构建。

本地候选结果和剩余线上门槛见
[`docs/cases/release-acceptance-2026-08-27.md`](docs/cases/release-acceptance-2026-08-27.md)。
