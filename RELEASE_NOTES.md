# OfferLoop 0.1.0-alpha.10

发布日期：2026-08-13

本版本将求职进展数据契约升级为 schema v6，并同步更新 9 个 Skill、安装验证、招聘信息同步、招聘提醒与配套飞书知识库契约。

## 主要变化

- 将 `进展状态` 设为求职进展的当前状态唯一真源。
- 新增 `公告链接`，并与求职企业清单逐值同步。
- 为 `最近完成节点` 增加 `面试完成`，保留群面、一面、二面、三面和 HR 面等细分节点。
- 移除业务 Base 中的 `投递简历版本`；简历选择只保留在具体简历任务上下文中。
- 对齐六张企业清单表的 `招聘批次` 选项。
- 新增 schema v6 在线验证脚本和迁移验收契约。

## 兼容性与迁移

- 已有 Base 可暂时保留 `当前阶段`、`下一环节`、`流程结果` 和 `当前状态`，仅供迁移及旧客户端兼容。
- 新逻辑优先读取 `进展状态`；该字段为空时才从兼容字段迁移，无法可靠判断时写入 `状态待确认`。
- 企业记录首次进入 `已投递` 时，默认创建 `待反馈 / 投递完成` 的求职进展。
- 安装和升级继续保持幂等，不复制已有 Base、知识库内容、本地用户文件或非敏感 locator。
- 配套飞书知识库仍是可选线上配置，不影响 9 个 Skill 的本地安装。

## 验证

- 求职进展 46 条记录完成 `公告链接` 回填并通过 schema v6 在线校验。
- 六张企业清单表的 `招聘批次` 选项一致。
- 七张相关表均已移除空字段 `投递简历版本`，删除前确认没有有效数据。
- GitHub Actions 在 Ubuntu、macOS 和 Windows 上完成冷安装检查。
- Python 3.10 与 3.12 全量测试通过。
- 招聘同步模板、进展同步服务及相关契约测试通过。

## 验收说明

本次迁移已在现有授权账号中完成真实 Base 结构、记录回填、知识库首页和线上同步服务验收。由于没有独立的新飞书账号，跨账号首次冷启动没有重新执行；该项由仓库所有者明确豁免。新用户首次接入仍应遵循 README 中的只读检查、授权确认和幂等初始化流程。
