# OfferLoop 0.1.0-alpha.16

候选日期：2026-09-02。此版本先经过开发仓与公开候选 CI；得到发布确认并完成发布门槛后再创建
prerelease。

本版本修复 `interview-prep` 自我介绍生成会被完整面试准备流程中的通用模板和后续岗位规则覆盖
的问题，不改变飞书 schema、资源创建、权限操作或业务写入代码。

## 修复

- 自我介绍改为独立阶段 A：只读取专用方法 reference 和用户确认的公司、JD、简历、经历等事实
  材料，生成后冻结。
- 完整面试准备的阶段 B 只有在自我介绍冻结后才读取其他 reference；模板、质量门禁、回答框架、
  岗位画像和 AI 产品指南均不得回写已冻结内容。
- 已有用户确认版优先作为底稿；除非用户明确要求推倒重写，不因重新调用 Skill 另写一版。
- 正式自我介绍统一为自然口述 1—2 分钟，不再使用固定汉字数代替试读计时。
- 新增自我介绍方法 reference，并移除其私人来源定位符；更早的无关 `test-results.md` 不进入仓库
  或安装包。

## 验证

- Skill quick validator 与 5 项 interview-prep 分阶段读取契约测试。
- Python 3.10/3.12 仓库测试、共享 AgentSkills 兼容性和 README 安装契约。
- macOS、Ubuntu、Windows 四 Agent 冷安装与确定性精简包验收；安装包包含新的方法 reference。
- 求职进展同步服务及活动应用模板的测试、类型检查与生产构建。
- 本候选未访问或写入真实飞书 Base、日历、群聊或用户数据；公开发布前仍需按发布清单完成必要
  的授权验收。

## 发布边界

- `v0.1.0-alpha.15` 的 tag、Release 和资产保持不变。
- 未经再次确认，不合并公开候选，不创建 `v0.1.0-alpha.16` tag 或 Release。
